# Learning Promotion contract

Promote durable rules only from reliable supporting evidence across independent source types. Project rules may promote from multiple independent evidence sources in one context. Global rules additionally require evidence from at least two distinct contexts. Reliable negative/conflicting signals reject rather than average away a rule.
