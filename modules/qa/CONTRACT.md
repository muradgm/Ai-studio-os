# QA runtime contract

QA must verify behavior at the user and system boundary, not merely repeat unit tests.

Required dimensions are derived from change risk and surface. Permission-sensitive changes require permission-boundary QA; state mutations require failure-recovery QA; UI changes require accessibility QA.

Missing required dimensions block release.
