import fs from 'node:fs/promises';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { chromium } from 'playwright';
import { fingerprintCreativeValue } from '../modules/creative-intelligence-foundation/fingerprint.mjs';
import { buildVisualProofEvidence } from '../modules/style-frame/runtime.mjs';
import { buildAfterMatterCreativeWorldRecovery } from '../benchmarks/011-creative-motion-capability-dogfood/canonical-world-recovery/world-recovery.mjs';

const repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const recoveryRoot = path.join(repoRoot, 'benchmarks', '011-creative-motion-capability-dogfood', 'canonical-world-recovery');
const proofRoot = path.join(recoveryRoot, 'style-frame-proof');
const readJson = async (file) => JSON.parse(await fs.readFile(path.join(repoRoot, file), 'utf8'));
const rel = (file) => path.relative(repoRoot, file).split(path.sep).join('/');
const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[char]);
const content = { object: 'Object 27 / repaired vessel', trace: 'Repair seam / 1988', history: 'The join remains visible as evidence of continued use, not as a flaw to hide.', reading: 'Care is neither a footnote nor a decorative finish. It is the record of how an object stayed in the world.', visit: '16 October 2026 - 18 January 2027 / Berlin / Tickets' };

const grammars = {
  'friction-index': { id: 'trace-index-composition', consequences: ['ordered trace index has permanent spatial authority', 'active factual trace branches into history beside the object', 'reading holds after the trace relationship is established', 'mobile keeps one active trace and an explicit return route'], theme: { bg: '#151311', ink: '#f4eee5', muted: '#aaa198', line: '#574f47', accent: '#c85545' } },
  'repair-ledger': { id: 'conservation-ledger-composition', consequences: ['object and conservation ledger coexist as equal evidence', 'dated annotations accumulate in chronological order', 'long-form reading is native to the ledger column', 'mobile recomposes into an object-context plus chronology stack'], theme: { bg: '#eee9df', ink: '#24211d', muted: '#746c62', line: '#a8a094', accent: '#9a473a' } },
  'pressure-room': { id: 'pressure-release-composition', consequences: ['one object occupies a compressed encounter field', 'a visible threshold separates encounter from released history', 'bypass and visit routes stay available before the threshold', 'mobile communicates pressure through sequence without elapsed time'], theme: { bg: '#11120f', ink: '#f0e9dd', muted: '#aaa397', line: '#4c4e48', accent: '#c17855' } }
};

function study() { return '<figure class="study"><div class="plane"></div><figcaption>NON-DOCUMENTARY PLACEHOLDER / MATERIAL STUDY</figcaption></figure>'; }
function header() { return '<header><b>AFTER MATTER</b><nav>EXHIBITION / OBJECTS / VISIT</nav><a href="#tickets">Tickets</a></header>'; }
function label(world, moment) { return `<div class="proof-label">STYLE-FRAME PROOF / ${esc(world.label)} / ${esc(moment.label)} / NO WORLD SELECTED</div>`; }
function objectPanel(className = '') { return `<section class="object ${className}">${study()}<p>${esc(content.object)}<br><span>Object context persists.</span></p></section>`; }

function friction(world, moment) {
  const index = `<aside class="trace-index"><b>TRACE INDEX</b><ol><li>Fading / 1976</li><li class="active">${esc(content.trace)}</li><li>Wear / 2004</li><li>Care note / 2012</li></ol><a href="#object">Return to object</a></aside>`;
  const history = `<article class="trace-history"><small>SELECTED FACTUAL TRACE</small><h1>${esc(content.trace)}</h1><p>${esc(content.history)}</p><hr><p>${esc(content.reading)}</p></article>`;
  const object = objectPanel();
  if (moment.id === 'exhibition-entry') return `<main class="friction entry">${label(world, moment)}${index}<section class="entry-object">${object}</section><section class="proposition"><h1>Time, read through what endured.</h1><p>${esc(content.history)}</p><p class="visit">${esc(content.visit)}</p></section></main>`;
  if (moment.id === 'object-detail-history') return `<main class="friction detail">${label(world, moment)}${index}${object}${history}</main>`;
  if (moment.id === 'curatorial-reading') return `<main class="friction reading">${label(world, moment)}${index}<article class="long"><small>FROM TRACE TO HISTORY</small><h1>What the seam can tell us.</h1><p>${esc(content.reading)} ${esc(content.reading)}</p><p>${esc(content.history)}</p></article><aside class="reading-object">${object}</aside></main>`;
  if (moment.id === 'visit-ticketing') return `<main class="friction tickets">${label(world, moment)}${index}<section class="ticket-object">${object}</section><section class="ticket-action"><small>VISIT / IMMEDIATE ROUTE</small><h1>See the evidence in person.</h1><p>${esc(content.visit)}</p><button>Reserve tickets</button><p>Access, opening hours and location remain outside the trace path.</p></section></main>`;
  return `<main class="friction mobile">${label(world, moment)}<div class="mobile-top">AFTER MATTER <a href="#tickets">Tickets</a></div>${object}<div class="active-trace">01 / ${esc(content.trace)}</div>${history}<a class="back" href="#index">Back to trace index</a></main>`;
}

function ledger(world, moment) {
  const entries = '<ol class="entries"><li><time>1976</time><span>Surface fading recorded</span><em>Observed</em></li><li class="active"><time>1988</time><span>Repair seam retained</span><em>Care</em></li><li><time>2004</time><span>Wear stabilized</span><em>Reviewed</em></li><li><time>2012</time><span>Provenance uncertain</span><em>Open</em></li></ol>';
  const panel = `<section class="ledger-panel"><small>CONSERVATION LEDGER</small><h2>Care / repair / uncertainty</h2>${entries}</section>`;
  const object = objectPanel('ledger-object');
  const reading = `<article class="ledger-reading"><small>EDITORIAL RECORD</small><h1>Care made public.</h1><p>${esc(content.history)}</p><p>${esc(content.reading)}</p></article>`;
  if (moment.id === 'exhibition-entry') return `<main class="ledger entry">${label(world, moment)}<section class="ledger-intro"><h1>One hundred objects. Their care stays visible.</h1><p>${esc(content.visit)}</p></section>${panel}${object}</main>`;
  if (moment.id === 'object-detail-history') return `<main class="ledger detail">${label(world, moment)}${object}${panel}${reading}</main>`;
  if (moment.id === 'curatorial-reading') return `<main class="ledger reading">${label(world, moment)}${object}<section class="chronology">${panel}</section><article class="ledger-long"><small>READING IN THE RECORD</small><h1>What repair leaves behind.</h1><p>${esc(content.reading)} ${esc(content.reading)}</p><p>${esc(content.history)}</p></article></main>`;
  if (moment.id === 'visit-ticketing') return `<main class="ledger tickets">${label(world, moment)}<section class="visit-ledger">${panel}</section><section class="visit-action"><small>VISIT RECORD</small><h1>Plan a visit.</h1><p>${esc(content.visit)}</p><button>Reserve tickets</button><p>Opening hours, access and location are separate from conservation metadata.</p></section>${object}</main>`;
  return `<main class="ledger mobile">${label(world, moment)}<div class="mobile-top">AFTER MATTER <a href="#tickets">Tickets</a></div>${object}<section class="mobile-ledger">${panel}</section><a class="mobile-visit" href="#tickets">Dates, access and tickets</a></main>`;
}

function pressure(world, moment) {
  const object = objectPanel('pressure-object');
  const threshold = '<div class="threshold"><span>THRESHOLD</span><i></i><a href="#history">Open factual history</a><a class="bypass" href="#visit">Bypass to visit</a></div>';
  const history = `<article class="released"><small>BEYOND THE THRESHOLD</small><h1>${esc(content.trace)}</h1><p>${esc(content.history)}</p><p>${esc(content.reading)}</p></article>`;
  if (moment.id === 'exhibition-entry') return `<main class="pressure entry">${label(world, moment)}<section class="pressure-intro"><h1>Time is felt as resistance.</h1><p>${esc(content.visit)}</p><a href="#visit">Visit information</a></section>${object}${threshold}</main>`;
  if (moment.id === 'object-detail-history') return `<main class="pressure detail">${label(world, moment)}${object}${threshold}${history}</main>`;
  if (moment.id === 'curatorial-reading') return `<main class="pressure reading">${label(world, moment)}<section class="compressed"><small>ATTENTION</small><h1>Hold the encounter.</h1><p>${esc(content.history)}</p></section>${threshold}<article class="released-reading"><small>RELEASE / READING</small><p>${esc(content.reading)} ${esc(content.reading)}</p><a href="#visit">Visit information remains available</a></article></main>`;
  if (moment.id === 'visit-ticketing') return `<main class="pressure tickets">${label(world, moment)}<section class="visit-before"><small>BEFORE THE THRESHOLD</small><h1>Plan a visit.</h1><p>${esc(content.visit)}</p><button>Reserve tickets</button></section>${threshold}${object}</main>`;
  return `<main class="pressure mobile">${label(world, moment)}<div class="mobile-top">AFTER MATTER <a href="#tickets">Tickets</a></div><section class="mobile-pressure"><h1>Time is felt as resistance.</h1>${object}</section>${threshold}<section class="mobile-release">${history}</section></main>`;
}

function frameHtml(world, moment) {
  const grammar = grammars[world.id];
  const body = world.id === 'friction-index' ? friction(world, moment) : world.id === 'repair-ledger' ? ledger(world, moment) : pressure(world, moment);
  const t = grammar.theme;
  return `<!doctype html><html><head><meta charset="utf-8"><style>
  :root{--bg:${t.bg};--ink:${t.ink};--muted:${t.muted};--line:${t.line};--accent:${t.accent}}*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;background:var(--bg);color:var(--ink);font-family:Arial,Helvetica,sans-serif}body{overflow:hidden}a{color:inherit;text-decoration:none}header{height:64px;position:absolute;inset:0 0 auto;z-index:9;padding:22px 30px;border-bottom:1px solid var(--line);display:flex;justify-content:space-between;font-size:10px;letter-spacing:.14em}header nav{color:var(--muted)}button{font:inherit;border:1px solid var(--accent);background:none;color:inherit;padding:11px 16px;letter-spacing:.1em;text-transform:uppercase}.proof-label{position:absolute;top:82px;left:30px;font-size:9px;letter-spacing:.14em;color:var(--muted);z-index:7}small{font-size:10px;letter-spacing:.15em;color:var(--muted)}h1,h2{font-family:Georgia,'Times New Roman',serif;font-weight:400;line-height:.95;margin:0}h1{font-size:clamp(42px,5vw,78px)}h2{font-size:28px}p{font-size:15px;line-height:1.52}.study{position:relative;margin:0;border:1px solid var(--line);height:100%;min-height:220px;overflow:hidden;background:linear-gradient(130deg,#0a0a09,transparent 52%),repeating-linear-gradient(60deg,rgba(255,255,255,.09) 0 1px,transparent 1px 22px)}.plane{position:absolute;inset:17%;border:1px solid color-mix(in srgb,var(--accent) 72%,white);transform:skew(-22deg) rotate(23deg);box-shadow:0 0 45px var(--accent);background:radial-gradient(circle at 54% 48%,var(--accent),transparent 16%)}figcaption{position:absolute;bottom:10px;left:10px;font-size:8px;letter-spacing:.1em;color:#fff}.object p{font-size:11px;letter-spacing:.08em;margin:9px 0}.object span{color:var(--muted);font-size:10px}.mobile-top{display:none}
  .friction,.ledger,.pressure{position:absolute;inset:64px 0 0}.trace-index{position:absolute;left:30px;top:74px;bottom:38px;width:220px;border-right:1px solid var(--line);padding-right:22px}.trace-index b{font-size:10px;letter-spacing:.15em;color:var(--muted)}.trace-index ol{list-style:none;margin:34px 0;padding:0}.trace-index li{padding:13px 0;border-bottom:1px solid var(--line);font-size:12px}.trace-index li.active{color:var(--accent);padding-left:13px;border-left:2px solid var(--accent)}.trace-index a,.back{font-size:11px;text-decoration:underline;text-underline-offset:5px}.friction.entry .entry-object{position:absolute;left:285px;top:110px;width:35%;bottom:86px}.proposition{position:absolute;left:calc(285px + 39%);right:42px;top:195px}.visit{margin-top:35px;font-size:11px;letter-spacing:.1em;color:var(--muted)}.friction.detail .object{position:absolute;left:285px;top:100px;bottom:52px;width:36%}.trace-history{position:absolute;left:calc(285px + 40%);right:80px;top:180px}.trace-history p{max-width:430px}.trace-history hr{border:0;border-top:1px solid var(--line);margin:25px 0}.friction.reading .long{position:absolute;left:310px;top:145px;width:48%;bottom:50px}.long p{font-family:Georgia,'Times New Roman',serif;font-size:25px;line-height:1.32}.reading-object{position:absolute;right:38px;top:180px;bottom:100px;width:22%}.friction.tickets .ticket-object{position:absolute;left:285px;top:128px;bottom:96px;width:31%}.ticket-action{position:absolute;left:calc(285px + 36%);top:192px;max-width:520px}.friction.mobile,.ledger.mobile,.pressure.mobile{position:absolute;inset:0;padding:18px}.friction.mobile .proof-label,.ledger.mobile .proof-label,.pressure.mobile .proof-label{position:static;margin:0 0 14px}.friction.mobile .mobile-top,.ledger.mobile .mobile-top,.pressure.mobile .mobile-top{display:flex;justify-content:space-between;border-bottom:1px solid var(--line);padding-bottom:11px;font-size:10px;letter-spacing:.12em}.friction.mobile .object{height:215px;margin-top:18px}.active-trace{border-left:2px solid var(--accent);padding:12px;margin-top:18px;font-size:12px}.friction.mobile .trace-history{position:static}.friction.mobile .trace-history h1{font-size:29px}.friction.mobile .back{display:block;margin-top:17px}
  .ledger-object .study{height:100%}.ledger-panel{border-left:1px solid var(--line);padding-left:24px}.entries{list-style:none;margin:22px 0;padding:0}.entries li{display:grid;grid-template-columns:58px 1fr 58px;gap:10px;padding:13px 0;border-top:1px solid var(--line);font-size:12px}.entries li.active{color:var(--accent);border-left:2px solid var(--accent);padding-left:10px}.entries em{font-style:normal;color:var(--muted);font-size:10px}.ledger.entry .ledger-intro{position:absolute;left:54px;top:165px;width:33%}.ledger.entry .ledger-panel{position:absolute;left:41%;top:135px;width:27%;bottom:70px}.ledger.entry .ledger-object{position:absolute;right:54px;top:124px;width:25%;bottom:93px}.ledger.detail .ledger-object{position:absolute;left:54px;top:110px;width:29%;bottom:52px}.ledger.detail .ledger-panel{position:absolute;left:37%;top:132px;width:30%;bottom:70px}.ledger-reading{position:absolute;left:70%;right:54px;top:190px}.ledger.reading .ledger-object{position:absolute;left:54px;top:116px;width:24%;bottom:100px}.chronology{position:absolute;left:32%;top:116px;width:27%;bottom:55px}.chronology .ledger-panel,.visit-ledger .ledger-panel{height:100%}.ledger-long{position:absolute;left:64%;top:160px;right:54px}.ledger-long p{font-family:Georgia,'Times New Roman',serif;font-size:23px;line-height:1.32}.ledger.tickets .visit-ledger{position:absolute;left:54px;top:130px;width:31%;bottom:70px}.visit-action{position:absolute;left:42%;top:180px;width:31%}.ledger.tickets .ledger-object{position:absolute;right:54px;top:140px;width:20%;bottom:105px}.ledger.mobile{overflow:hidden}.ledger.mobile .ledger-object{height:185px;margin-top:16px}.mobile-ledger{margin-top:15px}.ledger.mobile .ledger-panel{padding-left:15px}.ledger.mobile .ledger-panel h2{font-size:21px}.ledger.mobile .entries{margin:12px 0}.ledger.mobile .entries li{padding:8px 0}.mobile-visit{display:block;border-top:1px solid var(--line);margin-top:12px;padding-top:12px;font-size:12px}
  .pressure-object{position:absolute}.pressure-object .study{height:100%}.threshold{position:absolute;background:var(--accent);color:#1b1612;display:flex;flex-direction:column;justify-content:center;align-items:center;gap:17px;text-align:center;font-size:11px;letter-spacing:.1em}.threshold i{width:1px;height:64px;background:#1b1612}.threshold .bypass{font-size:10px;text-decoration:underline;text-underline-offset:5px}.pressure.entry .pressure-intro{position:absolute;left:50px;top:170px;width:33%;z-index:3}.pressure-intro a{font-size:12px;text-decoration:underline;text-underline-offset:5px}.pressure.entry .pressure-object{left:43%;right:6%;top:70px;bottom:0}.pressure.entry .threshold{left:39%;top:88px;bottom:0;width:64px}.pressure.detail .pressure-object{left:42px;top:75px;width:48%;bottom:45px}.pressure.detail .threshold{left:calc(42px + 50%);top:75px;bottom:45px;width:86px}.released{position:absolute;left:calc(42px + 50% + 125px);right:65px;top:205px}.released p{max-width:380px}.pressure.reading .compressed{position:absolute;left:50px;top:155px;width:29%;z-index:2}.compressed h1{font-size:64px}.pressure.reading .threshold{left:37%;top:75px;bottom:35px;width:140px}.released-reading{position:absolute;left:calc(37% + 180px);right:75px;top:190px}.released-reading p{font-family:Georgia,'Times New Roman',serif;font-size:26px;line-height:1.35}.released-reading a{font-size:12px;text-decoration:underline;text-underline-offset:5px}.pressure.tickets .visit-before{position:absolute;left:50px;top:180px;width:32%;z-index:3}.pressure.tickets .threshold{left:39%;top:0;bottom:0;width:84px}.pressure.tickets .pressure-object{left:48%;right:6%;top:75px;bottom:70px}.pressure.mobile{overflow:hidden}.mobile-pressure{margin-top:18px}.mobile-pressure h1{font-size:40px}.pressure.mobile .pressure-object{position:relative;height:190px;margin-top:18px}.pressure.mobile .threshold{position:relative;left:auto;top:auto;width:auto;height:80px;margin:0 -18px;padding:8px;display:grid;grid-template-columns:1fr 1px 1fr 1fr;gap:10px}.pressure.mobile .threshold i{width:1px;height:46px}.pressure.mobile .threshold a{font-size:9px}.mobile-release{margin-top:15px}.pressure.mobile .released{position:static}.pressure.mobile .released h1{font-size:25px}.pressure.mobile .released p{font-size:13px;margin:7px 0}
  .ledger.mobile .ledger-object{height:170px}.ledger.mobile .ledger-object .study{min-height:145px;height:145px}
  </style></head><body>${moment.viewport === 'mobile' ? '' : header()}${body}</body></html>`;
}

function binding(world, frame) {
  const grammar = grammars[world.id];
  return { worldId: world.id, compositionGrammar: grammar.id, compositionModelRef: fingerprintCreativeValue(world.compositionModel), interactionModelRef: fingerprintCreativeValue(world.interactionModel), responsiveStrategyRef: fingerprintCreativeValue(world.responsiveStrategy), renderedConsequences: grammar.consequences, viewport: frame.viewport };
}
async function render(browser, html, width, height, output) { const page = await browser.newPage({ viewport: { width, height }, deviceScaleFactor: 1 }); await page.setContent(html, { waitUntil: 'load' }); await page.screenshot({ path: output, type: 'png' }); await page.close(); }

const thesisPacket = await readJson('benchmarks/011-creative-motion-capability-dogfood/canonical-authority-recovery/creative-thesis-review-packet.json');
const historicalSelection = await readJson('benchmarks/011-creative-motion-capability-dogfood/runs/after-matter-phase0/selection.json');
const recovery = buildAfterMatterCreativeWorldRecovery({ thesisPacket, historicalSelection });
if (recovery.status === 'blocked' || !recovery.styleFramePlan?.reviewReady) throw new Error('After Matter Creative World recovery is not ready for browser proof.');
await fs.rm(proofRoot, { recursive: true, force: true });
for (const dir of ['source-html', 'frames', 'comparisons']) await fs.mkdir(path.join(proofRoot, dir), { recursive: true });
const browser = await chromium.launch({ headless: true });
const renderedFrames = [];
try {
  for (const frame of recovery.styleFramePlan.frames) {
    const world = recovery.exploration.worlds.find((item) => item.id === frame.worldId);
    const moment = recovery.styleFramePlan.moments.find((item) => item.id === frame.momentId);
    const source = path.join(proofRoot, 'source-html', `${frame.id}.html`);
    const image = path.join(proofRoot, 'frames', `${frame.id}.png`);
    await fs.writeFile(source, frameHtml(world, moment), 'utf8');
    await render(browser, await fs.readFile(source, 'utf8'), frame.width, frame.height, image);
    renderedFrames.push({ frameId: frame.id, worldId: world.id, momentId: moment.id, sourceRef: rel(source), imageRef: rel(image), worldRenderBinding: binding(world, frame) });
  }
  const comparisonRefs = [];
  const comparisonBindings = [];
  for (const moment of recovery.styleFramePlan.moments) {
    const frames = renderedFrames.filter((frame) => frame.momentId === moment.id);
    const grammarIds = frames.map((frame) => frame.worldRenderBinding.compositionGrammar);
    if (new Set(grammarIds).size !== 3) throw new Error(`Composition grammar collapsed for ${moment.id}.`);
    const tiles = await Promise.all(frames.map(async (frame) => ({ worldId: frame.worldId, image: (await fs.readFile(path.join(repoRoot, frame.imageRef))).toString('base64') })));
    const board = `<!doctype html><html><style>*{box-sizing:border-box}html,body{margin:0;background:#101010;color:#eee;font-family:Arial}main{padding:28px}h1{font:400 35px Georgia;margin:0 0 8px}.sub{font-size:11px;letter-spacing:.12em;color:#aaa;margin-bottom:22px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.tile{border:1px solid #474747;padding:8px}.tile b{display:block;font-size:10px;letter-spacing:.12em;margin-bottom:8px}.tile img{width:100%;height:${moment.viewport === 'mobile' ? '780' : '550'}px;object-fit:contain;display:block}</style><main><h1>${esc(moment.label)}</h1><div class="sub">SAME PRODUCT STATE / DISTINCT WORLD COMPOSITION GRAMMARS / NO WORLD SELECTED</div><div class="grid">${tiles.map((tile) => `<div class="tile"><b>${esc(tile.worldId)} / ${esc(grammars[tile.worldId].id)}</b><img src="data:image/png;base64,${tile.image}"></div>`).join('')}</div></main></html>`;
    const out = path.join(proofRoot, 'comparisons', `${moment.id}-comparison.png`);
    await render(browser, board, 1920, moment.viewport === 'mobile' ? 980 : 740, out);
    comparisonRefs.push(rel(out));
    comparisonBindings.push({ momentId: moment.id, compositionGrammars: grammarIds, distinct: true });
  }
  const visualProofEvidence = buildVisualProofEvidence({ plan: recovery.styleFramePlan, renderedFrames, comparisonRefs });
  if (!visualProofEvidence.reviewReady) throw new Error(`Incomplete visual proof: ${visualProofEvidence.findings.map((item) => item.code).join(', ')}`);
  const packet = { ...recovery, status: 'ready-for-human-creative-world-review', renderedFrames, comparisonRefs, comparisonBindings, visualProofEvidence, humanReviewPacket: { status: 'ready-for-human-creative-world-review', approvedThesisFingerprint: recovery.approvedThesisFingerprint, historicalPhase0Selection: 'Friction Index is historical evidence only; it does not select this canonical recovery.', worlds: recovery.exploration.worlds.map((world) => ({ worldId: world.id, renderingGrammar: grammars[world.id].id, strongestConsequence: world.signatureBehavior, principalRisk: world.unresolvedRisks[0], reducedMotionImplication: world.motionLanguage, mobileImplication: world.responsiveStrategy, practicalMuseumUsability: world.interactionModel })), comparisonRefs, selection: null }, truth: { ...recovery.truth, exactBrowserRaster: true, renderedCompositionBindingsValidated: true, humanVisualApproval: false, humanWorldSelectionConfirmed: false, selectedAutomatically: false } };
  await fs.writeFile(path.join(recoveryRoot, 'creative-world-review-packet.json'), `${JSON.stringify(packet, null, 2)}\n`, 'utf8');
  console.log(JSON.stringify({ frames: renderedFrames.length, comparisons: comparisonRefs.length, visualProofReady: visualProofEvidence.reviewReady, packet: rel(path.join(recoveryRoot, 'creative-world-review-packet.json')) }, null, 2));
} finally { await browser.close(); }
