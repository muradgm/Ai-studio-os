# Production Planning Contract

**Input:** explicit `prototype` or `production` mode plus a reusable recipe ID.

**Output:** final-use policy, truth/rights/accessibility/performance requirements, and provider-independent production stages/gates.

**Block:** unsafe production policy or unknown recipe. Prototype output is never final-use approved.
