# Benchmark History contract

Track benchmark outcomes by commit and time. Preserve historical pass-to-fail transitions, distinguish active regressions from recovered ones, and block current runtime health only when a regression remains active or history evidence is malformed.
